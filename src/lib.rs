// STK2ETH Workspace Root Library
// This library exists to provide a home for workspace-level integration tests
// The actual implementation is in the workspace members: ussdgeth, ussdclient, ethclient

#![doc = "STK2ETH - USSD to Ethereum Gateway System"]

// Re-export workspace members for convenience in tests
// Note: These are optional and only used if tests need them
